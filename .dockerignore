# Ignore the SQLite database file in Docker builds
# data/paddlepro.db

# Exclude reference code from deployments
reference_code/
*.bak
*.tmp
rally_backups/
scrapers/
scrapers_backups/

# React Native / iOS App (not needed on Railway server)
# Mobile app deploys via EAS to App Store, not Railway
ios/
.nvmrc

# Exclude large backup files from Docker builds (causes deployment timeouts)
*.sql
backup_*.sql
local_backup_*.sql
staging_backup_*.sql
backups/
backup_configs/ 